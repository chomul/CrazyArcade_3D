[/Script/EngineSettings.GeneralProjectSettings]
ProjectName=3D Crazy Arcade
ProjectVersion=0.1.0

[/Script/CrazyArcade3D.CA3DGameInstance]
; 접속 환경 설정 (Task 19). 게임 규칙이 아니므로 UCA3DRuleSet 이 아니라 여기에 둔다.
;
; ⚠️ EOS 보류 (2026-08-02 사용자 결정) — 목표가 "게임을 얼마나 빨리 만드느냐"이고 배포는 아직
; 미정이라, 로비/세션을 배포 시점으로 미뤘다. 접속은 IP 직접(`open <주소>:7777`)을 쓴다.
; 코드는 지우지 않고 꺼두기만 한다 — 나중에 이 값만 true 로 되돌리면 되도록.
; (단 엔진 쪽 Device ID 블로커가 먼저 해결돼야 한다 — mds/Tasks/19 "보류 상태" 절)
bEnableEOS=false
ArenaMapPath=/Game/Maps/L_Arena
; 로비 맵은 UI Task 에서 만든다. 비어 있으면 매치 종료 후 세션만 정리하고 트래블하지 않는다.
LobbyMapPath=
DefaultMaxPlayers=8
; EOS 세션 버킷 — 생성과 검색이 같은 값이어야 서로 보인다. 비워두면 EOS OSS 가 빌드 고유 ID 를
; 쓰는데, 그러면 에디터 빌드와 패키징 빌드가 서로의 방을 못 찾는다.
SessionBucketId=CrazyArcade3D
