# Checklist 10 — CA3DCharacter

> 대응 Task: `mds/Tasks/10-CA3DCharacter.md`
> **PIE를 실제로 돌리지 않은 항목은 체크하지 않는다 — 미검증으로 남긴다.**
> ⚠️ 이 Task의 완료 조건에는 **튜닝 값 사용자 확정**이 포함된다.

## 빌드 (필수 게이트)
- [ ] `CrazyArcade3DEditor` 빌드 통과
- [ ] `CrazyArcade3DServer` 빌드 통과
- [ ] 프로젝트 파일 재생성 실행

## 코드 검증 (정적)
- [ ] 커스텀 이동 코드 없음 — CMC 기본 사용
- [ ] 낙사 검사가 서버에서만 (`HasAuthority` 가드)
- [ ] `GetFootCell` 존재 + 공중 정의 미결정 주석 표기
- [ ] 튜닝 값이 하드코딩 아님 (프로퍼티/룰셋)

## 동작 검증 (PIE 필수 — 미실행 시 미검증)
- [ ] WASD 이동·점프 동작
- [ ] **계단식 접근로로 2층 도달 가능** (1블록 점프)
- [ ] 2블록 수직 벽은 점프로 못 오름
- [ ] 맵 밖 추락 → KillZ 로그 (사망 처리는 Task 12 연결 후)
- [ ] `GetFootCell` 로그가 서 있는 셀과 일치 (지상·점프 정점 각각 확인)
- [ ] (Listen+클라 1) 클라 이동이 부드럽다 (CMC 예측 정상)

## 튜닝 확정 (사용자 답변 필요 — 답변 전 미검증)
- [ ] 셀 크기 확정 (`AVoxelWorld::CellSize` 반영)
- [ ] 이동속도 확정
- [ ] 점프 높이 확정
- [ ] 공중 "발밑 셀" 정의 확정
