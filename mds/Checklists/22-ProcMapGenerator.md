# Checklist 22 — ProcMapGenerator

> 대응 Task: `mds/Tasks/22-ProcMapGenerator.md`
> **실제로 실행하지 않은 항목은 체크하지 않는다 — 미검증으로 남긴다.**

## 빌드 (필수 게이트)
- [ ] `CrazyArcade3DEditor` 빌드 통과
- [ ] `CrazyArcade3DServer` 빌드 통과
- [ ] 프로젝트 파일 재생성 실행

## 코드 검증 (정적 — 불변식 4)
- [ ] `FRandomStream(Seed)`만 사용 — `FMath::Rand/FRand` 없음
- [ ] float 연산 없음 (정수 높이맵·가중치)
- [ ] `TMap`/`TSet` 순회 없음, 컨테이너는 `TArray`
- [ ] 액터 이터레이션 없음 (순수 데이터 생성)
- [ ] 리롤 파생 시드가 고정식 (리롤도 결정론)
- [ ] 시도 상한 초과 시 false → 폴백 경로 존재
- [ ] `VoxelWorld` 교체가 1줄 (구조 결정 10)

## 동작 검증 (실행 필수 — 미실행 시 미검증)
- [ ] **같은 시드 5회 → 그리드 해시 전부 동일**
- [ ] 다른 시드 10개 → 서로 다른 맵, 전부 `Validate` 통과
- [ ] 모든 층 계단식 접근로 보장 (검사 ① 통과로 확인)
- [ ] 리롤 로그: 시드별 시도 횟수·실패 사유 출력, 리롤률 보고
- [ ] 강제 실패 시 `FallbackMapGenerator` 폴백 동작 1회 실증
- [ ] PIE: 절차 맵에서 이동·점프·폭발·낙하 정상
- [ ] (Listen+클라) 클라가 시드로 같은 절차 맵 재생성 — 해시 일치
- [ ] 리눅스 서버 ↔ 윈도우 클라 시드 재현 (VPS 검증 — 미실시면 미검증)
