# Checklist 22 — ProcMapGenerator

> 대응 Task: `mds/Tasks/22-ProcMapGenerator.md`
> **실제로 실행하지 않은 항목은 체크하지 않는다 — 미검증으로 남긴다.**

## 확정된 설계 (2026-08-06 사용자)
- [x] 지형 기복 = **산·협곡까지, Z 6층** — 웨딩케이크 계단식 구조물(층당 1칸 인셋),
      구조물 사이 틈이 협곡 역할
- [x] 맵 크기 = **인원별 티어**, 값은 룰셋 DataAsset — `SmallMatchMaxPlayers`(4) 이하
      `MapSizeSmall`(17×17×6), 초과 `MapSizeLarge`(21×21×6)
- [x] **파괴:고정 = 8:2 이상** ("부서지는 게 압도적이어야 맵 부수는 느낌") —
      **구조물(산)은 전부 Destructible** 로 확정. Immortal 은 외곽 벽 + 평지 기둥 격자
      (폭발 차단 골격 — 원작 정체성)뿐. 실측 79~84% 파괴, 테스트가 최악 시드 기준 75% 이상을 강제
- [x] ⚠️ 파생 수정: **사분면 편차 기준을 아이템 수 비례로** — `max(룰셋 값 4, N/4)`.
      구조물이 Destructible 이 되며 아이템이 11→71개로 늘자 절대값 4는 확률적으로 거의
      불가능해져 기준 시드가 리롤 상한 16회를 전부 소진했다 (실측). 생성 결과에만 의존하므로 결정론 유지

## 빌드 (필수 게이트) — 2026-08-06 직접 실행
- [x] `CrazyArcade3DEditor` 빌드 통과 (`Result: Succeeded`, 번역 단위 병합 강제)
- [x] `CrazyArcade3DServer` 빌드 통과 (동일)
- [x] 프로젝트 파일 재생성 실행

## 코드 검증 (정적 — 불변식 4) — grep 스캔으로 직접 확인
- [x] `FRandomStream(Seed)`만 사용 — `FMath::Rand/FRand` 0건
- [x] float 연산 없음 (정수 높이맵·가중치·`(X+Y)/42` 정수 스케일)
- [x] `TMap`/`TSet` 0건, 컨테이너는 `TArray`
- [x] 액터 이터레이션 없음 (순수 데이터 생성)
- [x] 리롤 파생 시드 고정식 `Seed * 7919u + attempt` (리롤도 결정론)
- [x] 시도 상한(`ProcRerollMaxAttempts`=16) 초과 시 false → `VoxelWorld` 가 폴백 전환.
      **생성기 실패도 결정론** — 같은 입력이면 어느 머신이든 똑같이 실패하므로
      서버·클라가 같은 폴백 경로를 탄다 (한쪽만 폴백을 타는 상황이 없다)
- [x] `VoxelWorld` 의 생성기 교체 — 절차 우선 → 실패 시 폴백 (GDD 4.2 안전장치 3)
- [x] 아이템 배치는 폴백과 **공용 헬퍼**(`FMapGenUtil::PlaceItemsInDestructibles`) —
      두 생성기의 아이템 규칙이 조용히 갈라지는 것을 구조로 방지
- [x] **`GridSize` 복제** — 클라가 로컬 인원수로 크기를 유추하면 중간 접속자가 다른 맵을
      만든다 (알려진 함정 "중간 접속"과 동형). 크기는 서버 결정·복제만 믿고, 미도착 시
      next-tick 재시도. 레거시 단일 인자 `ServerInitFromSeed(Seed)` 는 기존 동작 유지

## 동작 검증 — 자동화 (2026-08-06 · **전체 21스위트 실패 0** 직접 실행)
- [x] **같은 시드 5회 → 그리드 해시 전부 동일** — Seed 20260806 (21,21,6): 해시 `FC080461`,
      스폰 8·아이템 71·시도 1회 (구조물 Destructible 확정 후 값)
- [x] 다른 시드 10개 → **고유 해시 10/10**, 전부 `Validate` 통과 (생성기의 유효 기준으로 재검증)
- [x] 두 티어 모두 생성·검증 통과 — (17,17,6) `D05F587B` / (21,21,6) `2D04B0C1`
- [x] 모든 층 계단식 접근로 (검사 ① 통과 + z≥2 구조물 존재 확인 — 평지 퇴화 방지)
- [x] **재질 비율** — 시드 5개 최저 75% 파괴 (기준 75% 이상 강제), 대형 맵 실측 84%
- [x] 리롤 통계: 시드 10개 총 시도 21회 (**시드당 평균 2.1회, 리롤률 ~52%**).
      기준(50%)을 살짝 넘지만 주 원인이 ⑤ 하한(N/4)의 확률 요동이라 생성 규칙 문제가 아니다 —
      아이템 밀도(ItemDropPercent) 확정 시 재측정
- [x] 강제 실패(거리 999 룰셋) → false + 상한 16회 소진 — 폴백 신호 실증
- [x] 스폰 8개 전부 IsStandable + 스폰 링 위

## `-game` 실전 세션 (2026-08-06 직접 실행)
- [x] **티어 선택 동작** — `ca3d.BotFill 5` → "예상 5명 → 대형 (21,21,6)" 로그.
      절차 맵에서 봇 매치 정상 진행 (폭탄 14/14 막힘 승격, 아이템 노출·획득, 에러·ensure 0, 폴백 0)
- [x] ⚠️ **-ExecCmds 타이밍 함정 수정** — cvar 는 GameMode BeginPlay 이후에 적용되므로
      티어 판정이 봇 목표를 0으로 봤다 (첫 실측에서 실제 발생: 봇 5대인데 소형 맵).
      커맨드라인 폴백으로 수정. **`FParse::Value` 를 커맨드라인에 직접 쓰면 안 된다** —
      내부 `Strifind` 가 bSkipQuotedChars=true 라 `-ExecCmds="..."` **따옴표 안을 건너뛴다**.
      `-ExecCmds=` 값을 먼저 꺼낸 뒤 그 안에서 찾아야 한다

## 남은 검증 (미실행 — 체크 금지)
- [ ] PIE: 절차 맵에서 이동·점프(1칸 오르고 2칸 못 오름)·폭발·낙하 체감 —
      특히 Z 6층 구조물에서 카메라·가림 페이드가 잘 지내는지
- [ ] (Listen+클라) 클라가 시드로 같은 절차 맵 재생성 — 해시 일치
- [ ] 리눅스 서버 ↔ 윈도우 클라 시드 재현 (환경 부재 — 미실시)
- [ ] 서든데스 낙하가 6층 지형에서도 정상 (top solid 탐색은 이미 Z 무관이지만 눈 확인)
